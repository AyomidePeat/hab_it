  Row(mainAxisAlignment: MainAxisAlignment.spaceBetween,
                    children: [
                      CustomElevatedButton(
                          onPressed: decrement,
                          height: 40,
                          width: 50,
                          child: const Icon(Icons.remove)),
                          Padding(
                            padding: const EdgeInsets.all(8.0),
                            child: Text('$freq', style: bodyText1(context),),
                          ),
                           CustomElevatedButton(
                          onPressed: increment ,
                          height: 40,
                          width: 50,
                          child: const Icon(Icons.add)),
                    ],
                  )